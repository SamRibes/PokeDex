import React from 'react';
import { render, screen } from '@testing-library/react';
import { PokeContext } from './PokeContext';

// The context provider here really isn't doing all that much.
// We will test that it's working in the user flow tests

test('Renders the PokeContext Component', () => {});
